# Palimpsest — точка входа для Claude

Генеративная «живая карта» в духе Джерри Грецингера: смертный тикающий мир,
которым правит **колода рунных карт**, а законы мира — **скрижали на том же
языке** (метациркулярность в духе eval Бёрда). Полный замысел —
`PROJECT_BRIEF.md`, устав — `TheRuler.md`, эстетика — `AESTHETICS.md` +
`palette.ron`, дизайн шага — `~/.claude/plans/i-want-to-get-zippy-hamming.md`.
Общение по-русски, идентификаторы в коде — английские.

## Что сделано (третий шаг)

Рунный язык карт (S-выражения, сетка-сигил выброшена), скрижали-законы,
исполняемые абсолютно честно, сердцебиение колоды (игра ведёт ритмом),
Атлас миров как одна растущая Вещь, карт-бланш за прожитый мир, вехи
глубины, голос свидетеля. Плюс всё из шага 2: тикающий смертный мир,
поселения, цикл смерти/наследия, мета-карты, жесты, выборы, мутация.

## Рунный язык (`core/src/rune/`)

```
Expr ::= Atom | "(" Expr* ")"        Atom ::= Rune | Num
```

Смысл — позицией, как в Лиспе: голова верхнего списка — **материя**;
вложенный список с **глаголом** в голове — клауза-поведение; `(⏱ N …)` —
ритм (пульсы; пульс возрождает мёртвый исток); `(⌛ N)` — срок жизни;
атомы верхнего уровня — дефолты для всех клауз.

- материи: `~` вода · `♠` древо · `^` камень · `,` луг · `░` пустота ·
  `#` очаг · `⌂` руина
- глаголы: `∩` течь · `Y` ветвиться (число = ствол) · `∴` ползти (только
  по бумаге) · `×` грызть (только чужое; `♥` — живое, материя — её одну)
- прицелы: `↑↓←→` вектор · `→ M` к ближайшей M · `↷ M` обход/прочь от M
- время: `!` взрыв (×3/÷3) · `+` обилие
- законные (для скрижалей): `∈ ▢ ✶ ▲ ✺ ♡ ✋ ☾ † ▒ █`

Примеры: река `(~ (∩ ↓ ↷ ^) (⌛ 160))`, чума `(░ (× ♥ → #) (⏱ 40 !))`,
гейзер `(~ (⏱ 30 (∩ ↑ !)))`, роща-кольцо `(♠ (∴ ↷ ~))`. Компиляция
(`rune/compile.rs`) чистая и никогда не падает: мусор молчит, не-материя
в голове — карта «без следа». Мутация (`rune/mutate.rs`) — дрейф дерева:
руна→сосед семейства, число ±10–25%; голова-материя неприкосновенна.

## Скрижали (`core/src/tablet.rs`, `Tablets/*.md`)

Четыре закона на том же языке; дефолты создаются при первом запуске:

```
entropy    (░ (∈ ▢) (⏱ 2 ×) (⌛ 1200) (↷ ^ ⌂) (✺ 240))
hearth     (# (✶ 48 , (→ ~ 5) (→ ♠ 8)) (Y 90 (, 3) (♠ 1) (▲ 8)) (♥ 2) (→ ⌂))
heart      (♡ (⏱ 90) (✋ 3) (☾ 12))    — зов колоды, жесты, дрейф карт
threshold  († (▒ 55) (█ 82) (⌛ 40))    — пороги умирания и грация
```

Читатели (`EntropyLaw`/`HearthLaw`/`HeartLaw`/`ThresholdLaw`) — все поля
Option: **отсутствие клаузы = поведение не существует**. Выкинул `(⏱ 2 ×)` —
мир бессмертен; порвал Порог — мир не умирает официально. Экран ядра —
клавиша `t`; правка тем же крафтом, `reload_tablets()` действует на живом
мире. Законные руны в палитре открываются первой смертью мира: сначала
читать, потом писать.

## Сердцебиение и наследие

- `World::deck_calls()` — тик ≥ last_draw + N из Скрижали Сердца; клиент
  (`app::Pulse`) ведёт: Observing → Calling (рубашка дышит золотом) →
  Drawing (ритуал) → Aftermath (панель действий словами).
- **Атлас** (`Atlas/world-NNN.md` + `ATLAS.md`) — стопка умерших миров:
  эпилог, финальный снимок, законы эпохи, колода, вехи. Экран `a`, две
  вкладки (tab). Одна растущая Вещь.
- **Карт-бланш**: смерть мира даёт `legacy.blank_cards += 1`; в браузере
  колоды `n` — единственный способ создать карту с нуля.
- **Вехи** (`archivist::milestones`) — рекорды против предков; **кода
  свидетеля** в эпилоге каждой третьей эпохи держит вопрос «зачем» открытым.

## Архитектура (core headless, детерминизм строгий)

`crates/core`: `rune/{mod,parse,print,compile,mutate}`, `tablet`,
`sim/{seed,flow,branch,creep,gnaw,matter,entropy,settlement}` (Clause-driven;
entropy/settlement интерпретируют законы), `world` (step/draw/gesture/
apply_meta/deck_calls/reload_tablets), `event`→`archivist`, `cycle`,
`legacy`, `card` (```rune-блок, `write_rune` не трогает прозу), `deck`,
`save` (RON; законы не сохраняются — они производные от файлов).
Инвариант: HashMap плоскости не итерировать для поведения (plane.rs);
RNG = f(seed, tick|draw_count, соль).

`crates/tui` (ratatui): `app` (Mode + Pulse + Craft-редактор дерева),
`view/{map,chronicle,deck,craft,core,atlas,ritual,rune_text}`.
`rune_text` — общий рендер выражений: скобки цветом глубины, материи
краской тайла.

## Файлы данных

`Deck/*.md` (кириллические имена; `graveyard/` — забытые), `Tablets/*.md`,
`Atlas/`, `MAP.md`, `History.md`, `Chronicle/epoch-NNN.md`, `save.ron`,
`legacy.ron`. Всё рождается при первом запуске TUI.

## Как пользоваться

```bash
cargo run -p palimpsest-tui   # играть (из корня, где Deck/)
cargo run -p palimpsest-core --example rune_lab -- '(~ (∩ ↓))' 120 [x y]
cargo run -p palimpsest-core --example headless -- 6
cargo test                    # 31 снапшот/смоук-тест
```

Observe: `space` тяга · `p` пауза · `1`/`4` скорость · `i` жесты ·
`c` колода · `t` ядро · `a` атлас · `q` выход. Крафт: стрелки по дереву
(←→ соседи, ↓ внутрь, ↑ наружу), `tab` руна, `space` положить/снять,
`a` добавить рядом, `(` обернуть, `x` удалить, `0-9` число, `w` вписать.

## Добавить руну

1. Вариант в `rune::Rune` (+ `ch`/`from_char`/`family`, при материи —
   `as_matter` + `TileKind` + `plane::glyph` + `sim::matter` + цвета в tui).
2. Семантика: клауза-глагол → модуль в `sim/` + ветка `Seed::step`;
   модификатор → `rune/compile.rs`; законная руна → читатель в `tablet.rs`.
3. Анлок в `legacy::witness`/`absorb_death` (или стартовый набор).

## Принципы

Маленькие работающие шаги; язык должен учить (малое изменение — связный
дрейф); законы исполняются буквально, без защиты от бога; тьма — руны
безымянны в UI, знание из наблюдений; не фиксировать мироустройство раньше
пользователя; перед разрушительными действиями — спрашивать.

## Вне рамок (не блокировать)

Зеркало (мир→Expr, интерпретатор назад), куайны/твины (глагол «отпечатать
своё выражение материей» + чтение узора назад), полная автономия колоды,
существа-пиксели, WASM, 3D/Bevy.
